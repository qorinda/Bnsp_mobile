<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lsp_qorinda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LSP_Qorinda">
        <activity android:name=".Pengaturan"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".TambahPemasukan" />
        <activity android:name=".TambahPengeluaran" />
        <activity android:name=".DetailCashFlow" />
        <activity android:name=".ListCashflow" />
        <activity android:name=".Registrasi" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>